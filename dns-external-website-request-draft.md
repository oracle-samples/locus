# DNS Admin — New External Website Request (Draft)

**Project:** Locus — Open Source Agentic AI Framework SDK
**Repo:** https://github.com/oracle-samples/locus (UPL-1.0)
**Internal source:** https://orahub.oci.oraclecorp.com/saas-observ-eng/locus
**Submitter:** federico.kamelhar@oracle.com
**Drafted:** 2026-04-28

> Approval flow: Corporate Architecture, Marketing, Global Information Security.
> SLA: 48–72h (2–3 business days). No escalation path.
> Communications channel: dnsapprovals_us_grp@oracle.com — do **not** call individual approvers; do **not** contact Oracle DNS team about approval status.

> ## 🛑 BLOCKER — do not submit yet
>
> The `oracle-samples/locus` repository is currently **private** (created 2026-04-27, `visibility: private`, no successful Pages build yet).
> GitHub Pages on a private repo only serves to authenticated GitHub users with repo access — public DNS pointing at `oracle-samples.github.io` will not resolve to the docs until the repo is made public.
>
> **Sequence required:**
> 1. **GH-5931** (OGHO Category 2 distribution approval) closes → repo becomes public on `oracle-samples`
> 2. Push to `main` → docs workflow builds successfully → confirmed at `https://oracle-samples.github.io/locus/`
> 3. *Then* submit this DNS Admin form
>
> Submitting now would burn the 48–72h approval window on a target that is not reachable yet.

---

## Form fields

### IT group supporting this application
**OIT OCI (Hosts in oraclecloud/oracleiaas/oc-test)** *(pre-selected)*

> ⚠️ Heads-up: docs are actually hosted on **GitHub Pages**, not OCI. If the dropdown has an "External / GitHub" option, pick that. Otherwise leave as-is and clarify in *Additional comments*.

### IP Address or Subnet

> **Preferred answer for a subdomain target (e.g. `locus.oracle.com`):** CNAME, not A records.
> GitHub Pages docs explicitly recommend a `CNAME` to `oracle-samples.github.io` for any subdomain. A records are recommended only when pointing an apex/root domain. The CNAME also auto-follows if GitHub ever changes their anycast addresses.
>
> If the form supports a CNAME field, use:
> ```
> CNAME → oracle-samples.github.io
> ```

**Fallback (if the form requires IPs)** — GitHub Pages anycast IPs (verified 2026-04-28 via `dig +short oracle-samples.github.io`):

```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

These four anycast addresses serve every GitHub Pages site by Host header. They will work for both apex and subdomain custom domains. Mention the CNAME preference in *Additional comments* either way.

### Hostname / common name
**`locus.oracle.com`** *(or alternates — see "Open questions" below)*

### Line of business VP
**`<TODO: VP first.last@oracle.com>`**

> Manager (per SCSARCH-3729) = Sriram Vegaraju. Need the VP above Sriram who signed off on the OSS release.

### Business owner / project manager
`federico.kamelhar@oracle.com`

### Business justification and purpose
> Locus is an open-source software development kit Oracle is publishing on GitHub for the developer community. This site hosts the project's documentation: a static, read-only set of HTML pages that explain how to install and use the kit, with reference material, tutorials, and example walkthroughs.
>
> The site contains documentation only. There is no login, no user data is collected, no application logic runs, and no Oracle internal information is published. The audience is external software developers evaluating the kit and Oracle engineers contributing to it. Making the documentation available on an Oracle-branded URL gives the project the credibility and discoverability needed to attract community adoption, which is a stated business objective for this open-source release.

### New, Legacy, or Acquired company service
**New**

### Accessible (a11y)?
**Yes** — site is static HTML generated by MkDocs Material, which follows WCAG 2.1 AA practices (semantic HTML, keyboard navigation, ARIA labelling, color contrast, screen-reader compatibility). Confirm with your accessibility owner if formal sign-off is required.

### Integrated with Oracle SSO?
**No** — public documentation, no authentication.

### GIS / CSARB / GIT reviewed?
**Yes**

- **SCSARCH-3741** — *Locus - Open Source Agentic AI Framework SDK* — **Approved** 2026-04-27
- SCSARCH-3729 — Security Review intake (companion ticket)
- SCSARCH-3728 — OSSA verification

Full security assessment with re-run after remediation is attached to SCSARCH-3741.

### CSSAP Number
**N/A** — this is sample code distributed via GitHub (`oracle-samples` org), not a deployed production service. CSSAP applies to production solutions; the equivalent assurances for this OSS distribution are:

- **OGHO Category 2 distribution approval — GH-5931** ("Approval to distribute code locus Agentic Generative AI")
- Security review via **SCSARCH-3741** (above)

### Has your initiative been approved as required by CSSAP?
**Not applicable — see CSSAP Number field.** If the form forces Yes/No, answer **No** and reproduce the explanation above.

### OS / App Server / Application technology
> Static HTML documentation only. Generated locally with **MkDocs** (Python) using the **Material** theme; resulting plain HTML/CSS/JS files are published to the `oracle-samples/locus` repository's `gh-pages` branch and served by **GitHub Pages**. No operating system instance, no application server, no backend, no database — only static files behind GitHub's CDN, with HTTPS via Let's Encrypt-issued certificates.

### Additional comments
> - Repository: https://github.com/oracle-samples/locus (UPL-1.0)
> - Internal source: https://orahub.oci.oraclecorp.com/saas-observ-eng/locus
> - Approved security review: **SCSARCH-3741** (Approved 2026-04-27)
> - OGHO distribution approval: **GH-5931** (Category 2)
> - Hosting model: GitHub Pages serving static HTML built by MkDocs. **For subdomain targets, GitHub recommends a CNAME to `oracle-samples.github.io` rather than A records** — please configure CNAME if your tooling supports it. The four anycast IPs in the form (`185.199.108–111.153`) are GitHub's standard fallback that work for every Pages site.
> - No customer data, no PII, no SSO required, no exposed service endpoints, no inbound traffic to Oracle infrastructure.

---

## Open questions before submitting

1. **VP name + email** — TODO. Need the VP above Sriram Vegaraju who approved the OSS release.
2. **Hostname choice** — `locus.oracle.com` is generic and may attract Marketing/Corp-Arch pushback (the word "locus" by itself is broad). Lower-friction alternatives:
   - `locus-sdk.oracle.com`
   - `docs.locus.oracle.com`
   - `locusagents.oracle.com`
   - `docs.locusagents.oracle.com`
3. **IT group dropdown** — confirm whether there's an external/GitHub option to avoid mis-attributing the host.

## Reference tickets

| Ticket | Project | Status | Title |
|---|---|---|---|
| SCSARCH-3741 | jira.oraclecorp.com | Approved 2026-04-27 | Locus - Open Source Agentic AI Framework SDK |
| SCSARCH-3729 | jira.oraclecorp.com | Open | Security Review - Locus Open Source Agentic AI Framework |
| SCSARCH-3728 | jira.oraclecorp.com | Open | OSSA Verification - Locus Open Source Agentic AI Framework |
| GH-5931 | jira.oraclecorp.com | Open | Category 2: Approval to distribute code locus Agentic Generative AI |
| GH-5935 / 5937 / 5943 | jira.oraclecorp.com | Closed | OGHO publication subtasks |

## Background — alternative paths considered

- **`*.ai` external domain (e.g. `locusagents.ai`)** — available, ~$70–90/yr, but separate from Oracle's domain ownership; would require its own brand/legal approval if Oracle is the registrant. Quick path: register personally → CNAME to GitHub Pages → transfer to Oracle later.
- **`oracle-samples.github.io/locus`** — already live, requires no action.
- **Cloudflare-hosted on Oracle-owned TLD via CASAKAMAI** — heavier process, weeks of work; overkill for a static docs site.
