{
  "name": "locus-sandbox-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Plain-TypeScript front-end for the locus sandbox. Talks to the BFF, renders the playground.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
